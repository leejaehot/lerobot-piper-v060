# Copy to configs/rollouts/my_act.yaml and edit the values below.
profile:
  description: My lab · ACT
  # Uncomment when checkpoint camera keys differ from rollout.yaml.
  # camera_aliases:
  #   front: egoview
  #   wrist: wristcam

policy:
  default: my_act
  checkpoints:
    my_act: outputs/policies/my_act
  # Keep the checkpoint default unless a tested value is required.
  # n_action_steps: 100

rollout:
  task: Pick the object and place it in the container.
  # Override when this task has a different reset-grid guide.
  # setup_preview_config: ~/piper/configs/my_task_record.yaml
  # setup_preview_camera: egoview

# If demonstrations did not start at the shared rollout.yaml pose, copy the
# teleop_initial_pose section here and replace it with the dataset start pose.
